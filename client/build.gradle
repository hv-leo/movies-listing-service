description = "Create Dockerfile inside build dir and build image"

task buildImage(type:Exec) {
	commandLine "docker", "image", "build", "-f", "Dockerfile",  "-t", "${rootProject.ext.client_image_name}:${rootProject.ext.version}",  "./"
}